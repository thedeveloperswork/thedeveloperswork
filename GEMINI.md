# GEMINI.md

When processing requests, you MUST check the `.agents/workflows/` directory for operational directives related to your current task. 

**This file (`GEMINI.md`) is the core truth.** The workflows simply define specializations.

---

## 📚 Global Knowledge Base Protocol (Mandatory Data Sources)
All workflows operate by digesting the following modular data sources located in the `assets/` directory:
- **`01_profile.md`**: Global summary, technical arsenal (with recency timestamps), and digital footprint.
- **`02_experience.md`**: Quantified professional achievements and enterprise roles.
- **`03_projects.md`**: Technical deep-dives, timelines, and repository links.
- **`04_education.md`**: Official credentials, certifications, and specialized training.
- **`05_social.md`**: Approved structured layouts for all social, networking, and support links.

---

## 🧠 Global Operational Rules
These rules apply universally to ALL outputs generated by the agent, regardless of the workflow executed:

1. **The XYZ Formula (Impact Quantification):** Every generated bullet point must strictly adhere to: "Accomplished [X] as measured by [Y], by doing [Z]".
    - Examples: "Slashed execution time by 60%..." or "Achieved a 0% defect injection rate...". 
    - Eliminate subjective fluff; rely purely on technical metrics (%, time, TB, count).
2. **Strict Chronology:** Always order Projects, Experience, and Education in strictly descending order based on their timelines (most recent first).
3. **Linguistic Diversity:** Do not repeat the same action verb more than twice in any single generated document. Synthesize technical synonyms (e.g., Architected, Engineered, Orchestrated, Pioneered).

---

## 🛠️ Available Workflows

### 1. Portfolio Architect (`.agents/workflows/portfolio.md`)
- **Purpose**: Building a premium, state-of-the-art Next.js (React 19) portfolio website from scratch.

### 2. Resume Architect (`.agents/workflows/resume.md`)
- **Purpose**: Generating a strict, ATS-compliant LaTeX resume tailored to specific Job Descriptions.

### 3. GitHub Profile README (`.agents/workflows/readme.md`)
- **Purpose**: Updating and maintaining the `README.md` at the repository root.

---

## 🚀 CI/CD Management
All deployments are automated via GitHub Actions and are **only** triggered when a Pull Request is successfully merged into the target branch. A mandatory **Validate PR** guardrail runs on all open Pull Requests.

1. **Guardrail (`validate-pr.yml`)**
   - **Trigger**: Pull Request opened to `testing` or `master`.
   - **Checks**: Linting and basic validation (Next.js build checks).
   - **Goal**: Prevent broken code from being merged.

2. **Testing Environment (`testing` branch)**
   - **Trigger**: Merge to `testing`.
   - **Deployment**: Automatic build and SFTP deployment to the remote testing server.
   - **Required Secrets**: `SFTP_SERVER`, `SFTP_USERNAME`, `SFTP_PASSWORD`, `SFTP_REMOTE_PATH`.
   - **Caching**: Implements `actions/cache` for `.next/cache` and `~/.npm`.
   - **Assets**: Next.js `basePath` is dynamically derived from the remote deployment directory.

3. **Production Environment (`master` branch)**
   - **Trigger**: Merge to `master`.
   - **Deployment**: Automatic build and deployment to GitHub Pages (`gh-pages`).
   - **Assets**: Deployed to the repository's root URL path.

4. **Resume Pipeline**
   - **Status**: Manual generation. PDF compilation is removed from the automated CI/CD pipeline and should be handled locally using `pdflatex` to ensure exact tailoring before commit.